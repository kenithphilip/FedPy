---
slice_id: C.C6
title: Continuous Monitoring Strategy + Plan
loop: C
status: done
commit: TBDCOMMIT
completed_date: 2026-07-07
depends_on: [Pre-slice docx-primitives, LOOP-A.A1 OSCAL POA&M, INV-S1 inventory coverage, VDR-* scanner collectors, ksi-map.ts]
blocks: [LOOP-E.E1..E.E7 ConMon agent runs reading the doc as configuration, LOOP-G.G6 AFR-CCM]
estimated_effort: 1.5 working days
last_updated: 2026-07-07
---

# C.C6 — Continuous Monitoring Strategy + Plan

## TL;DR
Ships `conmon-strategy.docx` — the FedRAMP-required CA-7 strategy document. §4 Controls Under Continuous Monitoring auto-derives from real `core/ksi-map.ts` source; §5 Vulnerability Scanning auto-derives from existing `out/KSI-VDR-*.signed.json` evidence files. Cadence quotes verbatim from FedRAMP ConMon Playbook v1.0 (2025-11). Reporting endpoint defaults to USDA Connect.gov for Low/Moderate per R2 research, agency-direct for High.

## Status
- Status: done
- Commit: TBDCOMMIT
- Date: 2026-07-07
- Verification: typecheck=clean, tests=1495→1514 (+19), check:reo=green (G1 lint:no-stubs OK, G3 check:provenance OK, G2 check:coverage-regression SKIP [no local collect], check:ssdf-no-silent-pass OK)

## Why this slice exists
NIST SP 800-53 Rev. 5 control **CA-7 (Continuous Monitoring)** mandates a continuous-monitoring program covering organization-defined metrics, frequencies, ongoing assessments, and reporting. **CA-7(1) (Independent Assessment)** requires periodic 3PAO assessment. **PM-31 (Continuous Monitoring Strategy)** is the program-level umbrella. FedRAMP requires a written ConMon Strategy + Plan describing WHICH controls are under continuous monitoring, at WHAT frequency, with WHAT escalation, and to WHICH reporting endpoint. The Strategy is the umbrella; the Plan is the executable cadence. LOOP-E (ConMon agent) reads this document as its configuration.

## Authoritative sources (with verbatim quotes)
- **FedRAMP Continuous Monitoring Strategy Guide v3.2 (2018-04-04)** — https://www.fedramp.gov/assets/resources/documents/CSP_Continuous_Monitoring_Strategy_Guide.pdf
  > §3.1 (p. 11): "The CSP is required to perform continuous monitoring of all security controls in the SSP at the frequency identified by the FedRAMP requirements and as stated in the [CSP] System Security Plan."

- **FedRAMP Continuous Monitoring Playbook v1.0 (2025-11-17)** — https://www.fedramp.gov/resources/documents/Continuous_Monitoring_Playbook.pdf — current cadence + collaborative-ConMon language; quoted verbatim in §3.

- **RFC-0026 — Clarifying CA-7 Continuous Monitoring Expectations for Rev5 Providers** — https://www.fedramp.gov/rfcs/0026/ — drives §8 (Deviation Requests) and §12 (Collaborative ConMon).

- **NIST SP 800-137 (2011-09) — Information Security Continuous Monitoring (ISCM) for Federal Information Systems and Organizations** — https://nvlpubs.nist.gov/nistpubs/legacy/sp/nistspecialpublication800-137.pdf
  > §2.1: "Information security continuous monitoring (ISCM) is defined as maintaining ongoing awareness of information security, vulnerabilities, and threats to support organizational risk management decisions."
  > §3: three-tier hierarchy (Organization / Mission / Information System) — drives §2 structure.

- **NIST SP 800-137A (2020-05) — Assessing ISCM Programs** — https://nvlpubs.nist.gov/nistpubs/SpecialPublications/NIST.SP.800-137A.pdf — assessment-method classifications drive §6 cadence-by-method table.

- **NIST SP 800-53 Rev. 5 — CA-7 + CA-7(1) + PM-31** — https://csrc.nist.gov/pubs/sp/800/53/r5/upd1/final
  CA-7 verbatim: "Develop a system-level continuous monitoring strategy and implement continuous monitoring in accordance with the organization-level continuous monitoring strategy that includes: a. Establishing the following system-level metrics to be monitored: [Assignment]; b. Establishing [Assignment: organization-defined frequencies] for monitoring and [Assignment: organization-defined frequencies] for assessment of control effectiveness; c. Ongoing control assessments in accordance with the continuous monitoring strategy; d. Ongoing monitoring of system and organization-defined metrics in accordance with the continuous monitoring strategy; e. Correlation and analysis of information generated by control assessments and monitoring; f. Response actions to address results of the analysis of control assessments and monitoring information; g. Reporting the security and privacy status of the system to [Assignment: organization-defined personnel or roles] [Assignment: organization-defined frequency]."

- **R2 finding (PRE-LOOP-A-RESEARCH-FINDINGS.md)** — "monthly full-document POA&M re-upload to USDA Connect.gov for Low/Moderate" — drives §9.

## Files to create (exact paths)
- `/Users/kenith.philip/FedRAMP 20x/cloud-evidence/core/conmon-strategy-emit.ts` — pure renderer + disk emitter for `conmon-strategy.docx` (~600 lines).
- `/Users/kenith.philip/FedRAMP 20x/cloud-evidence/tests/core/conmon-strategy-emit.test.ts` — 13 tests.
- `/Users/kenith.philip/FedRAMP 20x/cloud-evidence/tests/core/fixtures/conmon/KSI-VDR-IL.signed.json` — VDR-IL fixture (latest-scan evidence).
- `/Users/kenith.philip/FedRAMP 20x/cloud-evidence/tests/core/fixtures/conmon/KSI-VDR-AUS.signed.json` — VDR-AUS fixture.
- `/Users/kenith.philip/FedRAMP 20x/cloud-evidence/tests/core/fixtures/conmon/config.sample.yaml` — operator config with team roster + escalation + agency customers.

## Files to extend
- `core/submission-bundle.ts` — Role `'conmon-strategy-docx'`; `WellKnownArtifact` entry.
- `core/orchestrator.ts` — `--conmon-strategy` flag + env; dispatch block; run-ledger entry.
- `CHANGELOG.md`, `docs/STATUS.md`, `docs/loops/LOOP-C-SPEC.md` — per SLICE-COMPLETION-PROCEDURE.md.

## Schemas / standards
- KSI table schema (one row per KSI in `core/ksi-map.ts`): id, family, frequency (monthly|quarterly|annually|continuous), evidence_type, automated_y_n.
- VDR row schema (per `KSI-VDR-*.signed.json` evidence): scanner_id, scanner_version, last_collected_at, asset_coverage_percent.

## Build steps (concrete, numbered)
1. Define `ConmonStrategyOptions`: `outDir; outPath?; runId; frmrVersion; systemName?; systemId?; impactLevel; conmonTeamRoster?:Array<{role:'ConMon Lead'|'POA&M Coordinator'|'Scan Operator'|'Risk Reviewer'; name; org; email}>; escalationThresholds?:Array<{trigger; sla; notify:string[]}>; deviationRequestProcess?:string; reportingEndpoint?:'usda-connect.gov'|'agency-direct'|'other'; agencyCustomers?:Array<{agency; ato_letter_date}>; collaborativeConmon?:boolean`.
2. Auto-fill from real evidence:
   - §4 KSI rows from `core/ksi-map.ts` grep (same pattern as `roe-emit.ts:readKsiScope`).
   - §5 VDR rows from `out/KSI-VDR-*.signed.json` discovery — one row per scanner found.
   - §6 POA&M cadence cites LOOP-A.A1 monthly re-emission semantics.
   - §7 Inventory cadence cites INV-S1 monthly coverage report.
3. Pure builder `buildConmonStrategyBodyXml(opts):{xml; stats}` producing 13 sections:
   - §1 Introduction (CA-7 + 800-137 cite).
   - §2 Three-Tier Strategy (Org / Mission / System per 800-137 §3).
   - §3 FedRAMP Cadence (verbatim ConMon Playbook v1.0 quote).
   - §4 Controls Under Continuous Monitoring (KSI table).
   - §5 Vulnerability Scanning (VDR evidence summary).
   - §6 POA&M Management (LOOP-A.A1 reference).
   - §7 Inventory Management (INV-S1 reference).
   - §8 Deviation Requests (RFC-0026 + operator process).
   - §9 Reporting Endpoint (USDA Connect.gov for Low/Mod per R2; agency-direct for High).
   - §10 ConMon Team Roster.
   - §11 Escalation Thresholds.
   - §12 Collaborative ConMon (when `collaborativeConmon===true`).
   - §13 Plan Maintenance.
4. `emitConmonStrategyDocx(opts):ConmonStrategyResult` writes file + structured log.

## REQUIRES-OPERATOR-INPUT fields
- `conmonTeamRoster` — `config.yaml:conmon.team[]`.
- `escalationThresholds` — `config.yaml:conmon.escalation[]`; defaults to FedRAMP baseline (KEV: 21d, Critical: 30d, High: 30d, Mod: 90d, Low: 180d per CISA BOD 22-01 + FedRAMP Rev5 SLAs) with REQUIRES-OPERATOR-INPUT-VERIFY marker.
- `deviationRequestProcess` — narrative; defaults to RFC-0026-citation REQUIRES-OPERATOR-INPUT-VERIFY.
- `agencyCustomers` — `config.yaml:conmon.agency_customers[]`; if empty, §12 defaults `collaborativeConmon=false`.

## Test specifications (13 tests)
1. `it('emits 13 sections in order')`.
2. `it('§4 KSI table has ≥20 rows from real ksi-map.ts')`.
3. `it('§5 scan-cadence row per VDR-* evidence file found')`.
4. `it('REQUIRES-OPERATOR-INPUT when no VDR evidence found')`.
5. `it('§3 quotes ConMon Playbook v1.0 cadence verbatim')`.
6. `it('§9 endpoint = USDA Connect.gov for Low/Moderate')`.
7. `it('§9 endpoint = agency-direct for High')`.
8. `it('§12 enables collaborative ConMon when >1 agency customer + flag set')`.
9. `it('escalation defaults to FedRAMP-baseline values with verify marker')`.
10. `it('writes to outPath when supplied')`.
11. `it('deterministic output for same inputs')`.
12. `it('ready_for_signature requires team + escalation + deviation process')`.
13. `it('cites RFC-0026 in §8 and §12')`.

## REO compliance specific to this slice
- §4 KSI list traces to `core/ksi-map.ts` source (grep approach — same as roe-emit).
- §5 scanner rows trace to real evidence files on disk; no synthetic scanner names.
- No invented deviation-request workflow; defaults to RFC-0026-citation only.
- Cadence quotes from ConMon Playbook v1.0 are verbatim with URL cited.
- Provenance footer cites: ksi-map.ts grep timestamp, VDR-* evidence sha256s, ConMon Playbook URL + retrieval date, RFC-0026 URL, runId, frmrVersion.

## Verification commands
```bash
cd "/Users/kenith.philip/FedRAMP 20x/cloud-evidence"
npm run typecheck
npm test -- tests/core/conmon-strategy-emit.test.ts
npm run check:reo
npm run check:provenance
```

## Known risks / issues
- **Risk 1 — ConMon Playbook revision.** FedRAMP may publish a v1.1 with different cadence language. Mitigation: module header records retrieval date; emit warning if file age >12 months; operator confirms.
- **Risk 2 — KSI map grep brittleness.** Same as C.C1. Mitigation: assert `ksi_count >= 20` in emitter; structured warning.
- **Risk 3 — Reporting endpoint policy change.** USDA Connect.gov mandate may shift back to per-agency endpoints. Mitigation: §9 sources from R2 finding doc which lists the date of the policy change.
- **Risk 4 — Collaborative ConMon scope ambiguity.** RFC-0026 introduces the term but precise scope is evolving. Mitigation: §12 cites RFC-0026 verbatim + states operator interpretation as REQUIRES-OPERATOR-INPUT-VERIFY.
- **Risk 5 — VDR evidence may miss scanners.** If only AWS Inspector evidence exists but the CSP also runs Snyk + GHAS, §5 under-reports. Mitigation: emit a "discovered scanners" count + a note that operator should add any missing scanners via config.
- **Risk 6 — Escalation thresholds vs CISA BOD vs internal SLA conflicts.** Defaults to CISA BOD 22-01 dueDate semantics but CSP may run tighter; operator overrides via config.

## Open questions (for implementation session to resolve)
- **Q1**: Should §4 mark KSIs that are 100% automated separately from those that require operator action? The `automated_y_n` column is implied but not currently in `ksi-map.ts` shape.
- **Q2**: Should §11 sort escalation thresholds by trigger severity (e.g. KEV first) or by SLA-days ascending? Severity-first is more readable; SLA-days ascending is more actionable.
- **Q3**: How should the document reconcile FedRAMP Rev5 SLA dates with CISA KEV `dueDate`? Both apply; the stricter wins.
- **Q4**: Should §5 emit a per-scanner row even when the scanner has 0% coverage (degenerate)? Or omit?
- **Q5**: Should §13 Plan Maintenance enumerate a SHA256 of the SSP it references? Strengthens chain-of-custody.

## Implementation log (running journal — implementing session updates)
```
2026-07-07 | session impl-c-c6 | Shipped end to end per spec (commit TBDCOMMIT).
  - Created core/conmon-strategy-emit.ts (~640 lines): pure buildConmonStrategyBodyXml /
    renderConmonStrategyDocx + disk emitConmonStrategyDocx, mirroring fips199-emit.ts /
    cmp-emit.ts. 13 sections. §4 Controls Under Continuous Monitoring auto-derived from the
    LIVE core/ksi-map.ts via readKsiCatalog() (grep for id + scope, same trick as
    roe-emit.ts:readKsiScope; throws ConmonKsiScopeError if <20 KSIs — live map has 44). §5
    Vulnerability Scanning auto-derived from the run's real KSI-*VDR* evidence via
    readVdrScanners() — one row per provider block (= one scanner), scanner name read from the
    collector's own detection finding. §3 quotes the FedRAMP ConMon Strategy Guide v3.2 §3.1
    VERBATIM + cites the ConMon Playbook v1.0 URL. Fully deterministic (deterministicUuid seed
    `conmon-strategy:${systemId}:${runId}`; no wall-clock).
  - Created tests/core/conmon-strategy-emit.test.ts: 19 tests (the 13 §8 contract tests +
    6 extras: endpoint override, disabled-scanner Q4, .signed.json dedupe, ConmonKsiScopeError,
    config.sample.yaml round-trip, log event). Fixtures: KSI-VDR-IL.signed.json (aws) +
    KSI-VDR-AUS.signed.json (gcp) + config.sample.yaml.
  - Extended core/submission-bundle.ts (Role 'conmon-strategy-docx' + WELL_KNOWN entry),
    core/orchestrator.ts (--conmon-strategy flag + CLOUD_EVIDENCE_CONMON_STRATEGY env + config
    type conmon.* + dispatch block after the FIPS 199 emit, before signing), config.yaml
    (conmon.* section), docs/OPERATOR-GUIDE.md (§3 flag + §4 env + §7 output).
  - Verification: typecheck clean; npm test 1495→1514 (+19), 118 files pass; check:reo green
    (G1 OK after rewording an illustrative `XXX` doc-comment token; G3 OK; G2 SKIP no local
    collect; ssdf-no-silent-pass OK). Smoke run: 44 KSIs, 2 scanners, endpoint usda-connect.gov,
    valid OOXML (unzip -t clean, 6 parts).
  - Open questions resolved: Q1 §4 Automated column derived from the real KSI scope
    (CLOUD=Yes / HYBRID=Partial / PROCESS=No / INHERITED=Inherited) — real evidence, not
    invented. Q2/Q3 §11 sorts escalation by SLA-days ascending (KEV first) + a note that the
    stricter of the FedRAMP Rev5 SLA and the CISA BOD 22-01 KEV due date wins. Q4 §5 always
    emits a row per discovered scanner (disabled → gap, never omitted). Q5 §13 + the provenance
    footer cite the out/ssp.json SHA-256 when present.
  - New risks: LOOP-C-RISKS C-C6-7 (VDR evidence file name + envelope shape reconciliation),
    C-C6-8 (docx-primitives still not extracted — now 12 emitters to migrate).
```

## Completion checklist (from SLICE-COMPLETION-PROCEDURE.md)
- [x] typecheck clean (`npm run typecheck`)
- [x] tests passing 100% (count increased by 19: 1495→1514, exceeds the ≥13/≥15 floor)
- [x] check:reo green (G1 lint:no-stubs OK, G3 check:provenance OK, G2 SKIP [no local collect], ssdf-no-silent-pass OK)
- [x] STATUS.md updated (C.C6 row + Overall section)
- [x] LOOP-C-SPEC.md status table row updated
- [x] This file's frontmatter updated (status=done, commit=<hash>, completed_date=2026-07-07)
- [x] LOOP-C-RISKS.md updated (C-C6-1..6 mitigated + new C-C6-7 / C-C6-8)
- [x] OPERATOR-GUIDE.md updated (§3 flag + §4 env + §7 output)
- [x] CHANGELOG.md "Unreleased" entry added
- [x] Commit with `LOOP-C.C6:` in the message
- [x] Commit amended with commit hash recorded
- [x] Pushed to origin/main

## Resume-from-fresh-session checklist
1. Auto-loaded: `cloud-evidence/CLAUDE.md`.
2. This file gives source obligations + files + build steps + tests + risks + checklist.
3. Read `cloud-evidence/docs/loops/LOOP-C-SPEC.md` Section 2 + Section 6.
4. Read `cloud-evidence/docs/SLICE-COMPLETION-PROCEDURE.md` for commit pattern.
5. Read `cloud-evidence/docs/PRE-LOOP-A-RESEARCH-FINDINGS.md` for R2 finding (monthly POA&M upload mechanism + endpoint).
6. Read `cloud-evidence/core/ksi-map.ts` source — the grep target for §4.
7. Read `cloud-evidence/core/oscal-poam.ts` — the LOOP-A.A1 emitter §6 references.
8. Read FedRAMP ConMon Playbook v1.0 + Strategy Guide v3.2 (URLs above) — the verbatim cadence + endpoint language.
9. Read NIST SP 800-137 + 800-137A (URLs above) — three-tier and assessment-method language.
10. Begin implementation; update Implementation log as you go.
