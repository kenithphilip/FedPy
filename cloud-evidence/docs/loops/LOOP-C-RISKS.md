# LOOP-C — Risks Register

> Live document. Implementing sessions add entries during work; resolved risks stay in the file with `status=resolved` + resolution note. Cross-cutting risks apply to ALL slices in LOOP-C; per-slice risks are local supplements not duplicated from cross-cutting.

> Read this file alongside the per-slice docs (`docs/slices/C/C.C*.md`). The per-slice docs contain a "Known risks / issues" section; this file consolidates loop-wide risks + risks that span multiple slices.

> Last updated: 2026-07-07 (C.C6 shipped).

---

## Cross-cutting risks (apply to ALL slices in this loop)

| ID | Description | Severity | Mitigation | Status |
|---|---|---|---|---|
| C-X-1 | **OOXML primitives duplication.** Today `core/ssp-docx.ts` and `core/roe-emit.ts` each define `para`, `heading`, `table`, `fieldTable`, `stylesXml`, `CONTENT_TYPES`, `ROOT_RELS`, `DOC_RELS`. Adding 12 more emitters duplicates 80+ lines × 12 = unmaintainable. | high | Pre-slice `core/docx-primitives.ts` extracts shared helpers BEFORE C.C1 lands. Pre-slice has 8 tests. C.C1..C.C9 import from the shared module. | **deferred (2026-07-07)** — the pre-slice was NOT executed with C.C1; `cmp-emit.ts` keeps local OOXML constants like the 4 shipped docx emitters (per-slice §7 scope + anti-pattern rule forbid refactoring out-of-scope files). See C-C1-6 / C-C2-10 / C-C3-8. Now **13** emitters to migrate when the shared module is extracted (ssp-docx, roe-emit, section889-rep-docx, section889-report-docx, cmp-emit, iscp-emit, iscp-test-aar, irp-emit, irp-test-aar, pta-pia-emit, fips199-emit, conmon-strategy-emit, rms-emit). See C-C1-6 / C-C2-10 / C-C3-8 / C-C4-8 / C-C5-8 / C-C6-8 / C-C7-8. |
| C-X-2 | **REQUIRES-OPERATOR-INPUT marker variants.** Two distinct markers: `REQUIRES-OPERATOR-INPUT` (missing data) vs `REQUIRES-OPERATOR-INPUT-VERIFY` (inferred-not-confirmed). Risk of inconsistent application across emitters. | medium | Both constants exported from `core/docx-primitives.ts` as `TBD` + `TBD_VERIFY`. Every emitter MUST import (no hardcoded strings). Lint script `scripts/lint-no-stubs.mjs` recognizes both markers as legitimate (not stubs). | pending |
| C-X-3 | **Deterministic UUID + metadata.** Every emitter calls `deterministicUuid(slice, systemId, runId)` from `core/oscal.ts`. If `runId` changes (timestamp), output changes — violates byte-identical determinism in tests. | medium | Tests freeze `runId` to a fixed value (e.g. `'test-run-0001'`); production runs naturally vary. Document the test convention in `tests/README.md`. | pending |
| C-X-4 | **Signing pipeline integration.** Every .docx must go through `core/sign.ts` (Ed25519 + RFC 3161). Risk: an emitter forgets to call sign or the orchestrator skips signing. | high | Orchestrator central dispatch wraps EVERY `--*` emitter in the same signing block; integration test asserts every LOOP-C .docx has `<filename>.sig` + `<filename>.tsr` artifacts alongside. | pending |
| C-X-5 | **Submission bundler `Role` union explosion.** Adding 11 new `Role` values may collide with future role names. | low | Use `<slice>-docx` naming convention (e.g. `cmp-docx`, `iscp-docx`); roles namespace by document type. | pending |
| C-X-6 | **Orchestrator CLI flag explosion.** 9 new `--cmp`, `--iscp`, ..., `--baseline-config` flags + 11 envs. Risk: CLI becomes unwieldy; users want a single `--docx-pack` umbrella. | medium | Provide both per-doc flags AND a `--docx-pack` umbrella that enables all 9 slices when set. Document in README. Defer umbrella until all 9 slices ship. | pending |
| C-X-7 | **FedRAMP-published template updates.** FedRAMP may release new versions of the SSP Appendix G ISCP template, PIA template, FIPS 199 template, or publish a CMP template. | medium | Every emitter module header records the template URL + retrieval date. CI cron checks template URLs for HTTP 304 vs 200 monthly; opens an issue when a 200 appears (indicating revision). | pending |
| C-X-8 | **NIST publication updates.** SP 800-60 Rev. 2 (IWD as of Jan 2024), SP 800-128 R2 (possible), CSF 2.0 v1.1 (possible). | medium | Every emitter records `SOURCE_VERSION` constant + URL; revision is a follow-up slice. | pending |
| C-X-9 | **Hyperlink cross-references.** §X cross-links between LOOP-C docs (e.g. CMP §5 → baseline-config.docx) need OOXML `_rels/document.xml.rels` relationship entries. Current SSP-2 + RoE emitters emit text refs, not active hyperlinks. | medium | Pre-slice `core/docx-primitives.ts` ships a `hyperlink(href, text)` helper that produces both the run text + the relationship entry. All cross-references use this. | pending |
| C-X-10 | **Page-break + headers/footers absent.** Current OOXML primitives don't emit headers/footers (system name + page X of Y). 3PAOs may expect them. | low | Scope OUT of LOOP-C per SPEC §6.7. Add LOOP-C.C10 follow-up only if 3PAO feedback demands it. Document the deliberate omission in cover-letter caveats. | pending |
| C-X-11 | **Provenance footer absence in early prototypes.** Risk that an emitter is shipped without footer citing source SHA256s + URLs. | high | `check:provenance` CI guardrail validates every emit-field has a `provenance` entry; integration test asserts each LOOP-C .docx contains a `<w:footer>` block with sha256 references. | pending |
| C-X-12 | **Operator config schema drift.** `config.yaml` accumulates `cmp.*`, `iscp.*`, `irp.*`, `privacy.*`, `fips199.*`, `conmon.*`, `rms.*`, `auth_request.*`, `baseline_config.*` keys. Risk of typos silently being ignored. | medium | Define a single JSON schema for `config.yaml` (`scripts/config-schema.json`) and validate at orchestrator startup; emit warnings for unknown keys. | pending |
| C-X-13 | **Tracker DB schema deferral.** Several operator inputs (CCB roster, IR team roster, privacy responses) belong in the tracker DB but tracker forms haven't been added yet. | medium | C.C1..C.C9 accept inputs via `config.yaml` (committed to repo) as a stop-gap; LOOP-F follow-up adds tracker forms. Document the stop-gap in each slice's `Open questions`. | pending |
| C-X-14 | **Multi-CSO scaling.** Each emitter accepts `outDir` — fine for single CSO. For LOOP-H.H3 multi-CSO, the orchestrator iterates per-CSO and calls each emitter once per CSO. | low | Already designed in. No change to LOOP-C emitters. | pending |
| C-X-15 | **Operator-supplied PII in emitted docs.** Multiple slices include rosters/contacts with real names + emails (CMP §3 CCB, IRP §2 team, ISCP Appendix A, RMS §9 executive, cover letter §6). | high | Emitter never logs full PII (only counts); .docx is signed + bundled into tar with restricted ACL; document handling in CSP operational runbook. Don't include role-based addresses (info@) for external contacts where possible. | pending |
| C-X-16 | **Determinism vs `submissionDate` / `testDate` defaults.** If undefined, emitter must NOT inject `new Date()` (REO Rule 1.7). | medium | Every date opt defaults to runId-derived ISO date (deterministic per run). Explicit REQUIRES-OPERATOR-INPUT marker when no plausible default. | pending |
| C-X-17 | **Word/Office incompatibility.** OOXML strings hand-constructed; risk of Word complaining about malformed parts. | high | Integration test: emit each .docx, run `unzip -t` to verify ZIP integrity; pass through `validator.w3.org`-style XML validator for each part. Optionally test-render via LibreOffice headless in CI. | pending |
| C-X-18 | **`check:reo` guardrail false positives.** `lint-no-stubs.mjs` may flag "TBD" + "REQUIRES-OPERATOR-INPUT" as stubs. | medium | Allowlist `REQUIRES-OPERATOR-INPUT` + `REQUIRES-OPERATOR-INPUT-VERIFY` as legitimate non-stub markers in `scripts/lint-no-stubs.mjs`. Document allowlist in CLAUDE.md Rule 3. | pending |
| C-X-19 | **Baseline-config + AFR-SCG ordering.** C.C9 and LOOP-G.G5 share `core/scg-comparator.ts`. Whichever ships first owns the module. | medium | C.C9 ships the comparator first (it's a LOOP-C dependency); G.G5 reuses without modification. Document the ownership in C.C9 module header. | pending |
| C-X-20 | **C.C1 ↔ C.C9 dispatch order.** Orchestrator must emit C.C9 (baseline-config.docx) BEFORE C.C1 (cmp.docx) so CMP §5 cross-link resolves. | medium | Enforce in orchestrator dispatch logic; document in `LOOP-C-SPEC.md` Section 4 (already noted). Integration test asserts dispatch order. | pending |

---

## Per-slice risks

### C.C1 — Configuration Management Plan
- **C-C1-1**: Tooling-suggestion false-confidence in §10 (REQUIRES-OPERATOR-INPUT-VERIFY for cloud-native CM tooling). — MITIGATED (2026-07-07, `99c283a`): §10 inferred rows carry the distinct `REQUIRES-OPERATOR-INPUT-VERIFY` marker (a separate Confirmation column value from `operator-confirmed`); the operator drops the suffix by supplying `config.yaml: cmp.tooling[]`.
- **C-C1-2**: Inventory drift between emit time and 3PAO sample time — §4 freezes a snapshot. — MITIGATED (2026-07-07): §1 records the inventory content SHA-256 and §11 documents the re-emit-on-rebuild cadence.
- **C-C1-3**: KSI map grep brittleness — assert `ksi_count >= 20` in emitter. — MITIGATED (2026-07-07): `buildCmpBodyXml` throws `CmpKsiScopeError` when `readKsiScope()` returns <20 domains (live map has 44).
- **C-C1-4**: §5 cross-link to baseline-config.docx requires `_rels/document.xml.rels` entry (covered by C-X-9). — OPEN: shipped as a text reference (active-hyperlink helper deferred with the docx-primitives extraction; see C-C1-6).
- **C-C1-5**: FedRAMP publishes a CMP template later — must re-target. — OPEN: `buildCmpBodyXml` is pure + section-driven, so a re-target is a single-function refactor.
- **C-C1-6** (NEW 2026-07-07, `99c283a`): **Pre-slice `core/docx-primitives.ts` not extracted.** LOOP-C-SPEC §4 + cross-cutting C-X-1 called for a shared docx-primitives module before C.C1. Reality: the C.C1 per-slice-doc §7 "Files to create" scopes this slice to `core/cmp-emit.ts` only, and the anti-pattern rule forbids extending scope beyond §7 / refactoring files not listed. The four already-shipped docx emitters (`ssp-docx`, `roe-emit`, `section889-rep-docx`, `section889-report-docx`) each keep local OOXML constants (`para`/`heading`/`table`/`fieldTable`/`CONTENT_TYPES`/`ROOT_RELS`/`DOC_RELS`/`stylesXml`). C.C1 follows that precedent to avoid touching + re-verifying SSP-2/RoE byte-identical output outside its scope. **Impact:** C-X-1 (OOXML duplication) remains a future refactor; when it happens, `cmp-emit.ts` is one of the 5 emitters to migrate. Severity: medium. Status: pending (refactor deferred).
- **C-C1-7** (NEW 2026-07-07): **`deterministicUuid` is single-arg.** C-X-3 assumed `deterministicUuid(slice, systemId, runId)`; the real `core/oscal.ts` export takes one seed string. C.C1 composes the seed as `cmp:${systemId}:${runId}` — deterministic, no wall-clock. The `docProps/core.xml` `<dc:title>` carries the UUID (OOXML core-properties, not a `<w:title>` element — there is no such WordprocessingML element). Severity: low. Status: resolved.
- **C-C1-8** (NEW 2026-07-07): **`.docx` is not in `core/sign.ts` `SIGNED_EXTENSIONS`.** The run-manifest signer covers `.json`/`.xml`/`.pem`/`.md`/`.pdf` only, so `cmp.docx` (like `roe.docx`/`ssp.docx`) is a printable companion, not per-file `.sig`-signed. Integrity is anchored by the signed submission-bundle `INDEX.json`, which SHA-256s + Ed25519-signs every WELL_KNOWN file it includes (`sha256Hex(content)` + `bundle_sha256`). The per-slice-doc §schemas claim "passes through the sign.ts pipeline" is reconciled to this bundle-anchored model; `sign.ts` (not in the C.C1 §7 file list) was NOT modified. The "ksi-map grep timestamp" provenance was reconciled to `runId` (no `Date.now()`), preserving byte-deterministic output. Severity: low. Status: resolved.

### C.C2 — ISCP + Test AAR
- **C-C2-1**: RPL evidence shape drift across collector versions. — MITIGATED (2026-07-07, `e660109`): the narrow `KsiEvidence` interface (`iscp-emit.ts`) reads only `ksi_id`/`rollup.pass`/`collected_at`; `readKsiEvidence` is JSON-parse-safe (never throws, skips malformed/missing files), so new backup-feature fields don't break the emitter.
- **C-C2-2**: Vendor-contact PII leak in Appendix B (covered partially by C-X-15). — MITIGATED (2026-07-07): the emitter never logs contact details (only `vendor_contact_count`); Appendix B pulls vendor + role + SLA from `subprocessor-inventory.json` but leaves contact + phone as `REQUIRES-OPERATOR-INPUT` (the SA-9 inventory carries no PII contact fields to leak).
- **C-C2-3**: AAR signature blocks confused as digital signatures. — MITIGATED (2026-07-07): the §6 sign-off narrative states the wet/electronic signatures are captured out-of-band and the toolkit never auto-signs a human attestation (REO Rule 1.10); the four signature/date cells are explicit `REQUIRES-OPERATOR-INPUT` markers.
- **C-C2-4**: `testDate` non-determinism (covered by C-X-16). — MITIGATED (2026-07-07): `testDate` defaults to `REQUIRES-OPERATOR-INPUT` (never `new Date()`); the UUID seed is `iscp-test-aar:${runId}:${testDate}`, so identical inputs yield byte-identical output (test #10).
- **C-C2-5**: Appendix G template revision. — OPEN: `buildIscpBodyXml` is pure + section-driven, so a re-target is a single-function refactor (same posture as C-C1-5).
- **C-C2-6**: RPL-TRC test-evidence age (>365 days = stale). — RECONCILED (2026-07-07): an automated staleness comparison would require wall-clock `new Date()` (violates C-X-16 determinism), so §4.2 renders each evidence file's `collected_at` verbatim + a static reviewer note directing the operator to confirm the most-recent TRC evidence is within the annual CP-4 window. Automated staleness deferred to the tracker layer (LOOP-E.E7), which has a run clock.
- **C-C2-7** (NEW 2026-07-07, `e660109`): **RPL evidence file name.** The per-slice §5 named the inputs `KSI-RPL-*.signed.json`; the real collector output is `KSI-RPL-*.json` (`risk-score-emit.ts` + `conmon-report.ts` explicitly filter `.signed.json` out as a duplicate). `readRplEvidence` reads `KSI-RPL-<id>.json` first and `.signed.json` as a fallback; fixtures named `.json`. Severity: low. Status: resolved.
- **C-C2-8** (NEW 2026-07-07): **Appendix B source file.** The per-slice §5/§built-steps named `out/subprocessors.json`; the real J.J2 output is `subprocessor-inventory.json` (a `{rows:[…]}` envelope). `readSubprocessorContacts` reads the real name first, the spec name as a fallback, and tolerates both `{rows:[]}` and bare-array shapes. Severity: low. Status: resolved.
- **C-C2-9** (NEW 2026-07-07): **Envelope shape.** The per-slice §schemas assumed `findings[].metadata.last_collected_at`; `core/envelope.ts:EvidenceFile` carries the timestamp at the top level (`collected_at`) and the aggregate outcome in `rollup.pass`. The narrow `KsiEvidence` interface consumes those. Severity: low. Status: resolved.
- **C-C2-10** (NEW 2026-07-07): **Pre-slice `core/docx-primitives.ts` still not extracted** (extends C-C1-6 / C-X-1). The per-slice §7 scopes C.C2 to `iscp-emit.ts` + `iscp-test-aar.ts`; both keep local OOXML constants like the five shipped docx emitters. Now **6** emitters to migrate when C-X-1 lands. Severity: medium. Status: pending (refactor deferred).
- **C-C2-11** (NEW 2026-07-07): **Tracker `iscp_tests` capture deferred.** The per-slice §11 anticipated a tracker DB table + UI for operator scenario/roster capture landing with LOOP-E.E7; no tracker subsystem consumes these emitters yet, so structured input flows via `config.yaml#iscp.*` as a stop-gap (same posture as C-X-13). Severity: low. Status: pending (LOOP-E.E7 follow-up).

### C.C3 — IRP + Test AAR
- **C-C3-1**: Spec-version confusion (Rev. 2 vs Rev. 3). — MITIGATED (2026-07-07, `f521fe3`): `--irp-spec-version` (env `CLOUD_EVIDENCE_IRP_SPEC_VERSION` / `config.yaml: irp.spec_version`) selects `800-61r3` (default, current standard) or `800-61r2`; §5 Respond swaps between the six CSF 2.0 Functions and the withdrawn four-phase model, and the §1.3 Methodology narrative states which is in force + that Rev. 2 was withdrawn April 2025. Auto-detection from `out/ssp.json` was NOT added (Q1 — avoids added complexity; the flag is explicit).
- **C-C3-2**: FedRAMP ICP doc supersession. — MITIGATED (2026-07-07): the `core/irp-emit.ts` module header + the §9 Provenance footer cite the CSP_Incident_Communications_Procedures.pdf URL; §9 SLAs render as `REQUIRES-OPERATOR-INPUT-VERIFY` baseline rows so the operator confirms them against the live doc each emission. Automated file-age warning deferred (would require wall-clock — same determinism constraint as C-C2-6).
- **C-C3-3**: External-contact PII in §7 (covered partially by C-X-15). — MITIGATED (2026-07-07): §7 defaults use FedRAMP + CISA published role-based addresses (`info@fedramp.gov`, `report@cisa.gov`) — no personal PII; the Agency POC row defaults to `REQUIRES-OPERATOR-INPUT` (each agency customer's POC comes from its ISA/MOU); the emitter never logs contact details (only counts).
- **C-C3-4**: INR-RIR coverage thresholds (<95% emits warning). — MITIGATED (2026-07-07): §4 renders each detection source's ACTIVE/GAP status + the derived coverage percent (passing/total findings); a sub-95% coverage surfaces an explicit WARNING paragraph directing each GAP row to a POA&M item, so the IRP never papers over the detection gap.
- **C-C3-5**: CSF 2.0 phase drift across NIST revisions. — MITIGATED (2026-07-07): the six CSF Functions live in a single exported constant `CSF_2_0_PHASES` (NIST-published, REO Rule 3), used for both the §5 headings and their ordering; a NIST rename is a one-line change. The verbatim §2.1 quote + URL are cited in the module header.
- **C-C3-6**: AAR signature blocks confused as digital signatures (covered by C-C2-3). — MITIGATED (2026-07-07): the §7 sign-off narrative states the signatures are captured out-of-band and the toolkit never auto-signs a human attestation (REO Rule 1.10); the four signature/date cells are explicit `REQUIRES-OPERATOR-INPUT` markers.
- **C-C3-7** (NEW 2026-07-07, `f521fe3`): **INR-RIR evidence file name + envelope shape.** The per-slice §7 named the fixture `KSI-INR-RIR.signed.json` and the §5/§build-steps assumed a flat `evidence[].detection_source` / `evidence[].coverage_percent` / `evidence[].last_collected_at`. Reality: the real collector output is `KSI-INR-RIR.json` (the `.signed.json` variant is filtered as a duplicate — same as C-C2-7), and the `core/envelope.ts:EvidenceFile` carries detection capabilities as `providers[].findings[]` (`rule` + `passed` + `current_state.summary`) with the timestamp at the top level (`collected_at`), NOT a flat `evidence[]` array (same as C-C2-9). `readInrRirEvidence` reads `KSI-INR-RIR.json` first (`.signed.json` fallback), flattens `providers[].findings[]` into detection-source rows, and derives `coverage_percent` from the pass ratio. Fixture named `.json`. Severity: low. Status: resolved.
- **C-C3-8** (NEW 2026-07-07): **Pre-slice `core/docx-primitives.ts` still not extracted** (extends C-C1-6 / C-C2-10 / C-X-1). The per-slice §7 scopes C.C3 to `irp-emit.ts` + `irp-test-aar.ts`; both keep local OOXML constants like the six shipped docx emitters. Now **8** emitters to migrate when C-X-1 lands. Severity: medium. Status: pending (refactor deferred).
- **C-C3-9** (NEW 2026-07-07): **Tracker `irp_tests` capture deferred.** The per-slice §11 REQUIRES-OPERATOR-INPUT table anticipates operator scenario/roster capture moving to a tracker DB + UI with LOOP-E.E7; no tracker subsystem consumes these emitters yet, so structured input flows via `config.yaml#irp.*` as a stop-gap (same posture as C-X-13 / C-C2-11). Severity: low. Status: pending (LOOP-E.E7 follow-up).

### C.C4 — PTA + PIA
- **C-C4-1**: Rev5 PTA/PIA template release (currently absent). — OPEN (2026-07-07, `ed26d8d`): `buildPtaBodyXml`/`buildPiaBodyXml` are pure + section-driven, so a re-target to a future FedRAMP Rev5 template is a single-function refactor (same posture as C-C1-5/C-C2-5). The module header + both provenance footers pin the A04 template URL + retrieval date (2026-06-07); §1 states the Rev4-structure/Rev5-control-IDs reconciliation verbatim.
- **C-C4-2**: Inventory mis-tagging causing PTA-negative when PII present. — MITIGATED (2026-07-07, `ed26d8d`): when NO inventoried asset carries ANY `data_classification` tag, PTA §3 renders a `REQUIRES-OPERATOR-INPUT-VERIFY` warning that a PTA-negative determination cannot be trusted until assets are classified, and directs the privacy officer to confirm no PII-bearing asset was left untagged before signing.
- **C-C4-3**: PII enumeration leaks PII into emitted doc (§3 of PTA). — MITIGATED (2026-07-07, `ed26d8d`): `redactIdentifier` masks the resource-name segment (after the last `/` or `:`) to `***` and appends only a `ref:<sha8>` correlation hash; §3 shows Asset Type + Data Classification + the redacted reference. A smoke run confirmed the raw resource names (`acme-prod-customer-pii-store`, `acme-patient-records`) never appear in the emitted `document.xml`; a dedicated test asserts their absence.
- **C-C4-4**: `piaForceMode='never-emit'` inappropriate suppression. — MITIGATED (2026-07-07, `ed26d8d`): a positive PTA whose PIA is suppressed by `never-emit` renders §4 "DETERMINATION: … PIA emission was SUPPRESSED by the operator … `REQUIRES-OPERATOR-INPUT-VERIFY`: verify this suppression with the privacy officer — a positive PTA normally REQUIRES a PIA." The result carries `pia_suppressed=true` for the orchestrator console.
- **C-C4-5**: Rev4 vs Rev5 control-ID mismatch confuses 3PAOs. — MITIGATED (2026-07-07, `ed26d8d`): PTA §1 emits an explicit CONTROL-ID NOTE — "the Rev5 PT-2 / PT-3 / PT-6 controls are satisfied using the published Rev4 FedRAMP SSP Attachment A04 PTA/PIA template structure, because FedRAMP has not published a Rev5 PTA/PIA template (help-desk article 28907995813275) … The control identifiers below are Rev5; the document outline is Rev4."
- **C-C4-6**: SORN (PT-6) requires Federal Register process — PIA cannot satisfy by itself. — MITIGATED (2026-07-07, `ed26d8d`): PIA §4 carries a `sornReference` field defaulting to `REQUIRES-OPERATOR-INPUT (only if this is a Privacy Act system of records)` plus a verbatim note: "a PIA cannot by itself satisfy PT-6 — publishing a System of Records Notice is a Federal Register process the agency customer initiates. This PIA records the SORN reference; it does not create the SORN."
- **C-C4-7** (NEW 2026-07-07, `ed26d8d`): **`data_classification` is a free-form asset tag, not a hard enum.** The per-slice §7 "Files to extend" listed `core/inventory-emit.ts` — "confirm `data_classification` enum exposes `'pii'`/`'phi'` … verify in source." Verification shows `data_classification` is NOT a hard enum: it is a free-form string tag applied by the inventory enrichers / operator resource tags and read as `data_classification` (snake) by `risk-score.ts` and `dataClassification` (camel) by `inventory-workbook.ts`. `readPiiInventory` therefore reads it defensively (snake_case, camelCase, and the `DataClassification`/`classification`/`sensitivity` tag keys), lowercased, and `core/inventory-emit.ts` needed NO modification — the `pii`/`phi` values already flow through as free-form tags. Severity: low. Status: resolved.
- **C-C4-8** (NEW 2026-07-07, `ed26d8d`): **Pre-slice `core/docx-primitives.ts` still not extracted** (extends C-C1-6 / C-C2-10 / C-C3-8 / C-X-1). The per-slice files scope C.C4 to `pta-pia-emit.ts`; it keeps local OOXML constants like the eight shipped docx emitters. Now **10** emitters to migrate when C-X-1 lands (ssp-docx, roe-emit, section889-rep-docx, section889-report-docx, cmp-emit, iscp-emit, iscp-test-aar, irp-emit, irp-test-aar, pta-pia-emit). Severity: medium. Status: pending (refactor deferred).
- **C-C4-9** (NEW 2026-07-07, `ed26d8d`): **Tracker `privacy_responses` capture deferred.** The per-slice §REQUIRES-OPERATOR-INPUT table anticipates PTA/PIA response capture moving to a tracker DB table + UI as a future LOOP-E enhancement; no tracker subsystem consumes these emitters yet, so structured input flows via `config.yaml#privacy.*` as a stop-gap (same posture as C-X-13 / C-C2-11 / C-C3-9). Severity: low. Status: pending (LOOP-E follow-up).

### C.C5 — FIPS 199 worksheet
- **C-C5-1**: SP 800-60 Rev. 2 release requires catalog re-extract. — MITIGATED (2026-07-07, `bbfdaad`): `core/fips199-types.ts` exports a pinned `SOURCE_VERSION = 'SP 800-60 Vol. 2 Rev. 1'` (+ `SOURCE_URL`) cited in the module header + the worksheet §1/§2/footer; a Rev. 2 re-extract is a single-module bump. Rev. 2 remains IWD (Jan 2024), so Rev. 1 codes are the correct pin today.
- **C-C5-2**: Unknown info-type code passed in config. — MITIGATED (2026-07-07, `bbfdaad`): a code outside the SP 800-60 V2 subset is accepted (Q3: warn + accept) with an `UNKNOWN-TYPE-CODE` annotation in the §3 Code column + a `stats.unknown_type_codes[]` list surfaced on the orchestrator console; the operator confirms it against the full SP 800-60 Vol. 2 catalogue.
- **C-C5-3**: SSP MISMATCH false positives. — MITIGATED (2026-07-07, `bbfdaad`): §4.1 surfaces both values (SSP vs worksheet) + a per-objective agreement table + the likely cause (operator under-categorization of the §3 information types) + reconciliation guidance; a worksheet confidentiality of NOT APPLICABLE is treated as consistent with any SSP confidentiality (the OSCAL enum has no NOT APPLICABLE), so a public-data system is not falsely flagged.
- **C-C5-4**: `c=n/a` edge case in high-water-mark. — MITIGATED (2026-07-07, `bbfdaad`): `computeOverallSC` treats `n/a` as no-contribution to the confidentiality mark (RANK n/a = -1); when every type's confidentiality is `n/a` the system confidentiality is `n/a`; integrity + availability never accept `n/a` (validated — `Fips199ImpactError`). Test #3 pins the behaviour.
- **C-C5-5**: Operator under-categorization not detectable by worksheet. — MITIGATED (2026-07-07, `bbfdaad`): §2 Methodology states verbatim that completeness is the system owner's responsibility — the worksheet computes the high-water-mark over the §3 types supplied but cannot detect an omitted or under-categorized information type; the §4.1 MISMATCH note names under-categorization as a common cause.
- **C-C5-6**: Approver signature freshness across re-emits. — MITIGATED (2026-07-07, `bbfdaad`): §6 renders `categorizationApprover.date` verbatim (never `new Date()` — determinism, C-X-16) with an explicit note that the date is captured out-of-band and must be refreshed on each re-emission; the signature cell stays `REQUIRES-OPERATOR-INPUT` (RA-2.c — never auto-signed, REO Rule 1.10).
- **C-C5-7** (NEW 2026-07-07, `bbfdaad`): **SSP `security-impact-level` shape.** The per-slice §schemas + build-step 5 assumed `system-characteristics.security-impact-level` is a single OSCAL enum string (`fips-199-low|moderate|high`). The real emitted shape (`core/oscal-ssp.ts`) is a per-objective OBJECT — `security-objective-{confidentiality,integrity,availability}`, each `fips-199-<level>` — plus an overall `security-sensitivity-level` string. `crossReferenceSsp` reads the object shape (a richer per-objective CONSISTENT/MISMATCH comparison than the spec's overall-only check), derives the overall from `security-sensitivity-level` (falling back to the objectives' high-water-mark), and still tolerates the spec-assumed bare-string shape. Also handles the `{ 'system-security-plan': { 'system-characteristics': … } }` wrapper. Severity: low. Status: resolved.
- **C-C5-8** (NEW 2026-07-07, `bbfdaad`): **Pre-slice `core/docx-primitives.ts` still not extracted** (extends C-C1-6 / C-C2-10 / C-C3-8 / C-C4-8 / C-X-1). The per-slice §files scope C.C5 to `fips199-emit.ts` + `fips199-types.ts`; the emitter keeps local OOXML constants like the ten shipped docx emitters. Now **11** emitters to migrate when C-X-1 lands (ssp-docx, roe-emit, section889-rep-docx, section889-report-docx, cmp-emit, iscp-emit, iscp-test-aar, irp-emit, irp-test-aar, pta-pia-emit, fips199-emit). Severity: medium. Status: pending (refactor deferred).

### C.C6 — ConMon Strategy + Plan
- **C-C6-1**: ConMon Playbook v1.0 revision. — MITIGATED (2026-07-07, `a6ea46c`): the emitter pins `CONMON_PLAYBOOK_VERSION = 'v1.0 (2025-11-17)'` + `CONMON_PLAYBOOK_URL` in the module header, §1 identity table, §3, and the provenance footer; §3 renders the monthly cadence as a `REQUIRES-OPERATOR-INPUT-VERIFY` baseline directing the operator to confirm against the live Playbook (the clean-room tree does not reproduce the Playbook's prose — the verbatim quote used in §3 is the FedRAMP ConMon Strategy Guide v3.2 §3.1, which the per-slice §2 supplies). A file-age warning was NOT added (it would require wall-clock `new Date()`, violating C-X-16 determinism — same posture as C-C2-6/C-C3-2).
- **C-C6-2**: KSI map grep brittleness (covered by C-C1-3). — MITIGATED (2026-07-07, `a6ea46c`): `buildConmonStrategyBodyXml` throws `ConmonKsiScopeError` when `readKsiCatalog()` returns <20 KSIs (the live map has 44); the grep regex `^\s*'(KSI-[A-Z]+-[A-Z0-9]+)'\s*:\s*\{[\s\S]*?scope:\s*'([A-Z]+)'` is anchored to the map-key line, so it never matches the inner `id:` field or the special-cased KSI-AFR-PVA/KSI-CSX-SUM comment.
- **C-C6-3**: Reporting endpoint policy change (USDA Connect.gov ↔ per-agency). — MITIGATED (2026-07-07, `a6ea46c`): §9 defaults the endpoint from the impact level (Low/Moderate → USDA Connect.gov per the R2 finding; High → agency-direct), the operator overrides via `config.yaml: conmon.reporting_endpoint`, and §9 carries an explicit `REQUIRES-OPERATOR-INPUT-VERIFY` policy note that the Connect.gov mandate may shift back to per-agency endpoints.
- **C-C6-4**: Collaborative ConMon scope ambiguity (RFC-0026 evolving). — MITIGATED (2026-07-07, `a6ea46c`): §12 cites RFC-0026 verbatim (URL), enables collaborative ConMon only when the flag is set AND >1 agency customer, and marks the shared-deliverable interpretation `REQUIRES-OPERATOR-INPUT-VERIFY` so the operator confirms scope with each agency.
- **C-C6-5**: VDR evidence may miss scanners (under-reports). — MITIGATED (2026-07-07, `a6ea46c`): §5 emits one row per discovered VDR provider block (never omitting a disabled scanner — Q4), reports the `scanner_count`, and renders a note directing the operator to add any scanners (Snyk / GHAS / Wiz / Tenable) not auto-discovered; when no VDR evidence exists §5 renders a single `REQUIRES-OPERATOR-INPUT` row.
- **C-C6-6**: Escalation thresholds vs CISA BOD vs internal SLA conflicts. — MITIGATED (2026-07-07, `a6ea46c`): §11 defaults to the published FedRAMP Rev5 / CISA BOD 22-01 baseline (KEV 21d / Critical 30d / High 30d / Moderate 90d / Low 180d), sorted SLA-days ascending, with an explicit note that the stricter of the FedRAMP Rev5 SLA and the CISA KEV per-CVE due date wins (Q3); the operator tightens via `config.yaml: conmon.escalation[]` (the default carries a `REQUIRES-OPERATOR-INPUT-VERIFY` marker).
- **C-C6-7** (NEW 2026-07-07, `a6ea46c`): **VDR evidence file name + envelope shape.** The per-slice §7 named the fixtures `KSI-VDR-IL.signed.json` / `KSI-VDR-AUS.signed.json` and the §schema assumed a flat `{scanner_id, scanner_version, last_collected_at, asset_coverage_percent}` per scanner. Reality: the VDR collector registers as `KSI-AFR-VDR` (ksi-map.ts) and emits `KSI-AFR-VDR.json` (the `.signed.json` variant filtered as a duplicate — same as C-C2-7/C-C3-7), and `core/envelope.ts:EvidenceFile` carries scanners as `providers[].findings[]` (rule `<provider>.vdr.detection_capability_enabled` + `current_state.summary`/`observations`) with the timestamp at the top level (`collected_at`) and the counts in a `vdr.summary` evidence entry — there is NO `scanner_version`/`asset_coverage_percent` field. `readVdrScanners` discovers any `KSI-*VDR*.json` (the real `KSI-AFR-VDR.json` + the spec-named fixtures + `.signed.json` fallback, deduped preferring the plain `.json`), derives one row per provider block (= one scanner), reads the scanner name from the detection finding, detection-enabled from `finding.passed`, and findings/KEV from the `vdr.summary` data; scanner-version + per-asset coverage are surfaced as a `REQUIRES-OPERATOR-INPUT-VERIFY` note. Severity: low. Status: resolved.
- **C-C6-8** (NEW 2026-07-07, `a6ea46c`): **Pre-slice `core/docx-primitives.ts` still not extracted** (extends C-C1-6 / C-C2-10 / C-C3-8 / C-C4-8 / C-C5-8 / C-X-1). The per-slice §files scope C.C6 to `conmon-strategy-emit.ts` + its test/fixtures; the emitter keeps local OOXML constants (`para`/`heading`/`table`/`fieldTable`/`stylesXml`/`CONTENT_TYPES`/`ROOT_RELS`/`DOC_RELS`) like the eleven shipped docx emitters. Now **12** emitters to migrate when C-X-1 lands (ssp-docx, roe-emit, section889-rep-docx, section889-report-docx, cmp-emit, iscp-emit, iscp-test-aar, irp-emit, irp-test-aar, pta-pia-emit, fips199-emit, conmon-strategy-emit). Severity: medium. Status: pending (refactor deferred).

### C.C7 — Risk Management Strategy
- **C-C7-1**: LOOP-B not yet shipped — §5/§6/§10 degrade to REQUIRES-OPERATOR-INPUT. — MITIGATED (2026-07-08, `ea68fea`): §5 (risk-register.json, B.B5), §6 (the `.risk-acceptances.json` / `.compensating-controls.json` snapshots, B.B3/B.B4), and §10 (poam.json, A.A1) each degrade to a `REQUIRES-OPERATOR-INPUT` marker with a "Generate via LOOP-B / --oscal-poam" cross-link when absent (Open Q2: degrade, never block — REO Rule 4), and the orchestrator prints a clear "LOOP-B risk register not present" console warning when `--rms` runs standalone. In practice B.B3/B.B4/B.B5 + A.A1 are all shipped, so a full run auto-fills every section.
- **C-C7-2**: POA&M severity bucketing drift if LOOP-A.A1 changes enum. — MITIGATED (2026-07-08, `ea68fea`): `summarizePoam` coerces every poam-item severity through the explicit envelope enum `{critical, high, medium, low, info}` (case-insensitive) and throws the typed `PoamSeverityError` on any unknown value — no silent mis-bucket. A test seeds a `catastrophic` severity and asserts the throw.
- **C-C7-3**: `oldest_open_finding_age_days` non-determinism (covered by C-X-16). — MITIGATED (2026-07-08, `ea68fea`): the §10 "now" for the overdue + oldest-open-age computation is NEVER a wall-clock `new Date()`. It derives from the POA&M `metadata.last-modified` (an input datum carried in the poam.json the emitter already reads), and is injectable via `opts.now` for tests, so identical inputs yield a byte-identical `.docx`. (Note: the per-slice §9 Risk 3 mitigation named "runId ISO timestamp", but `runId` is a `randomUUID()`, not an ISO timestamp — reconciled to the POA&M last-modified, which is deterministic per input.)
- **C-C7-4**: Risk-tolerance values overspecified (single tolerance per CIA). — MITIGATED (2026-07-08, `ea68fea`): §8 renders the operator's single-level per-CIA tolerance and appends a footer note that a CSP with multi-tier nuance may add a free-text `config.yaml: rms.tolerance_narrative` — the emitter never invents a nuanced tolerance.
- **C-C7-5**: Cross-reference to ConMon Strategy (§7) dangling when C.C6 not in bundle. — MITIGATED (2026-07-08, `ea68fea`): §7 emits the `out/conmon-strategy.docx` cross-link only when that file is present in `outDir`; otherwise it renders a `REQUIRES-OPERATOR-INPUT` marker directing the operator to `--conmon-strategy`. A test asserts both branches.
- **C-C7-6**: Executive oversight PII (covered by C-X-15). — MITIGATED (2026-07-08, `ea68fea`): §9 executive-oversight names + orgs are operator-supplied via `config.yaml: rms.executive_oversight[]` (never CLI flags), and the emitter logs only `executive_oversight_count` — never the names. The `.docx` is signed + bundled into the tar with the same handling as the other roster-bearing LOOP-C docs.
- **C-C7-7** (NEW 2026-07-08, `ea68fea`): **POA&M severity enum + LOOP-B snapshot file names.** The per-slice §schema named `count_by_severity: {critical, high, moderate, low}` and §build-step-2 read `out/risk-acceptances.json` / `out/compensating-controls.json`. Reality: `core/envelope.ts:Severity` is `critical | high | medium | low | info` (the fourth bucket is `medium`, NOT `moderate` — `moderate` is a FIPS-199 impact level, not a finding severity), and the LOOP-B.B3/B.B4 snapshots are the signed DOTFILES `.risk-acceptances.json` / `.compensating-controls.json` (per `risk-acceptance-reader.ts:RISK_ACCEPTANCES_SNAPSHOT`), each an object with an `items[]` array — not a bare array, not the non-dotted names. `summarizePoam` buckets by the honest enum (all five, incl. `info`); `readAcceptancePolicy` reads the dotfile first and accepts the non-dotted form as a fallback, counting `doc.items` / `doc.entries` / a bare array defensively. Same spec-vs-reality posture as C-C5-7 / C-C6-7. Severity: low. Status: resolved.
- **C-C7-8** (NEW 2026-07-08, `ea68fea`): **Pre-slice `core/docx-primitives.ts` still not extracted** (extends C-C1-6 / C-C2-10 / C-C3-8 / C-C4-8 / C-C5-8 / C-C6-8 / C-X-1). The per-slice §files scope C.C7 to `rms-emit.ts` + its test/fixtures; the emitter keeps local OOXML constants (`para`/`heading`/`table`/`fieldTable`/`stylesXml`/`CONTENT_TYPES`/`ROOT_RELS`/`DOC_RELS`) like the twelve shipped docx emitters. Now **13** emitters to migrate when C-X-1 lands (ssp-docx, roe-emit, section889-rep-docx, section889-report-docx, cmp-emit, iscp-emit, iscp-test-aar, irp-emit, irp-test-aar, pta-pia-emit, fips199-emit, conmon-strategy-emit, rms-emit). Severity: medium. Status: pending (refactor deferred).

### C.C8 — Authorization request cover letter
- **C-C8-1**: Orchestrator dispatch order — cover letter after INDEX, before bundle tar (covered by C-X-20 pattern). — RESOLVED 2026-07-10 (`f669531`): see C-C8-9 — the shipped `emitSubmissionBundle` is monolithic, so the orchestrator runs a pre-pass bundle (strict:false) to materialize INDEX.json, emits the letter reading it, then the final bundle dispatch re-packs with the letter enclosed + enumerated in the final INDEX.json.
- **C-C8-2**: INDEX.json shape drift across LOOP-A.A4 versions. — MITIGATED 2026-07-10 (`f669531`): `readIndexJson` consumes only the narrow fields it needs from each artifact row (`filename`/`role`/`sha256`/`bytes`/`description`) + `run_id`, and tolerates the extra columns (`in_manifest`/`required`/future fields) A.A4 emits (Risk 2). See C-C8-7.
- **C-C8-3**: AP metadata absent (`ap.json` not run). — MITIGATED 2026-07-10 (`f669531`): §3 falls back to the operator-supplied 3PAO (`config.yaml#auth_request.tpa` / `--3pao-name`); only when BOTH the operator and `ap.json` are silent does §3 emit `REQUIRES-OPERATOR-INPUT` (Risk 3). See C-C8-8.
- **C-C8-4**: `submissionDate` non-determinism (covered by C-X-16). — MITIGATED 2026-07-10 (`f669531`): `resolveSubmissionDate` is pure — submissionDate → an ISO date embedded in the runId → a `REQUIRES-OPERATOR-INPUT` "dated at signing" marker; never a wall-clock read. Deterministic .docx (test #10).
- **C-C8-5**: AO addressee delegated designate handling. — MITIGATED 2026-07-10 (`f669531`): `AoAddressee.designee` is an optional field rendered as a "Delegated AO Designee" row in the Addressee block when present (Risk 5).
- **C-C8-6**: Cover letter signed twice (Ed25519 + wet/electronic exec signature). — MITIGATED 2026-07-10 (`f669531`): §7 renders the exec signature line only when the signatory is supplied; when absent it emits a `REQUIRES-OPERATOR-INPUT` note distinguishing the pipeline Ed25519 signature (over the bundle INDEX.json) from the separate wet/electronic exec signature — the toolkit never auto-signs the human attestation (REO Rule 1.10).
- **C-C8-7** (NEW 2026-07-10, `f669531`): **INDEX.json / ap.json schema drift vs the per-slice §schema.** The per-slice §schema named INDEX.json as `{artifacts, emitted_at, runId}`, but the REAL INDEX.json (`core/submission-bundle.ts`) is `{artifacts:[{filename, role, description, sha256, bytes, in_manifest, required}], built_at, run_id, gaps, chain_check, provenance, ...}` — `built_at`/`run_id`, not `emitted_at`/`runId`, and per-row `in_manifest`/`required`. `readIndexJson` reads `doc.run_id` + the narrow row fields and ignores the extras (same defensive posture as C-C7-7 / C-C5-7). Severity: low. Status: resolved (reader matches the real emitter).
- **C-C8-8** (NEW 2026-07-10, `f669531`): **AP metadata parties carry no per-party role + no named lead.** The per-slice §schema named AP `parties:[{name, org, role:'third-party-assessor-organization'|...}]`, but the REAL `ap.json` (`core/oscal-ap.ts`) records organization parties as `{uuid, type:'organization', name}` with NO role and NO lead-assessor person (the AP adds the CSP org first, the 3PAO org second). `resolveThirdParty` prefers the operator-supplied 3PAO, else the organization party whose name ≠ the CSP org (falling back to the second org party); the lead assessor comes only from operator config. Severity: low. Status: resolved (real-shape heuristic + operator precedence, tested).
- **C-C8-9** (NEW 2026-07-10, `f669531`): **`emitSubmissionBundle` is monolithic (INDEX + tar in one call) — the spec assumed a splittable INDEX-build / pack seam.** The per-slice §files called for the orchestrator to dispatch INDEX-build → cover-letter → bundle-pack, but the shipped bundler builds INDEX.json and packs the tarball in ONE atomic call, with no seam between. Rather than refactor `submission-bundle.ts` (out of C.C8 §files scope + anti-pattern rule), the orchestrator runs a **pre-pass** `emitSubmissionBundle` (strict:false) when `--auth-cover-letter` + `--submission-bundle` are co-run — materializing INDEX.json for the current run — then emits the letter, then the normal bundle dispatch RE-packs the tarball so the letter is enclosed + enumerated in the final INDEX.json. The interim pre-pass tarball is overwritten by the final pass (one extra bundle build; acceptable). Standalone (no `--submission-bundle`) the letter reads whatever INDEX.json is on disk (a prior run) and degrades §4. Severity: low. Status: resolved (two-pass dispatch realizes the spec's ordering intent).
- **C-C8-10** (NEW 2026-07-10, `f669531`): **Pre-slice `core/docx-primitives.ts` still not extracted** (extends C-C1-6 / C-C2-10 / C-C3-8 / C-C4-8 / C-C5-8 / C-C6-8 / C-C7-8 / C-X-1). The per-slice §files scope C.C8 to `auth-cover-letter-emit.ts` + its test/fixtures; the emitter keeps local OOXML constants (`para`/`heading`/`table`/`fieldTable`/`stylesXml`/`CONTENT_TYPES`/`ROOT_RELS`/`DOC_RELS`) like the thirteen shipped docx emitters. Now **14** emitters to migrate when C-X-1 lands (ssp-docx, roe-emit, section889-rep-docx, section889-report-docx, cmp-emit, iscp-emit, iscp-test-aar, irp-emit, irp-test-aar, pta-pia-emit, fips199-emit, conmon-strategy-emit, rms-emit, auth-cover-letter-emit). Severity: medium. Status: pending (refactor deferred).

### C.C9 — Baseline Configuration
- **C-C9-1**: Reference-arch.ts schema drift across providers. — MITIGATED 2026-07-11 (`TBDCOMMIT`): `readReferenceArchitecture` greps each `finding({rule, ... severity, ... target:{summary,rationale}, ... nist_controls:[...]})` block from source. If a provider file is refactored so the regex misses, that provider degrades to 0 entries + `readable:false`/coverage-gap footer (Risk 5) rather than crashing; §4 surfaces per-provider counts so a drop is visible. See C-C9-8.
- **C-C9-2**: AFR-SCG (LOOP-G.G5) not yet shipped — comparator owned by C.C9 (covered by C-X-19). — RESOLVED 2026-07-11 (`TBDCOMMIT`): `core/scg-comparator.ts` ALREADY EXISTS (shipped earlier), so C.C9 did NOT re-ship it. It compares an SCG settings-map to observed settings (the RECOMMENDED-config counterpart to this baseline-of-record); C.C9 §5's inventory-vs-reference deviation is a distinct concern computed by the local pure `diffInventoryVsReference`, and §2 Methodology cites scg-comparator as the sibling comparator. See C-C9-9.
- **C-C9-3**: Multi-cloud component-type normalization. — MITIGATED 2026-07-11 (`TBDCOMMIT`): `componentClassFor(assetType, resourceType)` maps the per-cloud native types to a stable class (serverless/container/database/storage/network/identity/compute/other) so §3 groups AWS EC2 + Azure VM + GCP Compute Instance under a common `compute` class. Documented in §2 Methodology; unit-tested (test: "componentClassFor normalization").
- **C-C9-4**: Deviation severity assignment must not default. — MITIGATED 2026-07-11 (`TBDCOMMIT`): §5 severity is taken from the anchoring reference finding's DECLARED `severity` (grep-captured from source), NEVER defaulted. `diffInventoryVsReference` emits a deviation row only when a real anchor exists; no anchor → no invented deviation. Tested (test #4 asserts medium/high from the real AWS anchors).
- **C-C9-5**: Cross-cloud reference-arch coverage gaps. — MITIGATED 2026-07-11 (`TBDCOMMIT`): §4 footer prints per-provider expectation counts + a "source not readable" note per cloud, and the provenance footer cites each `reference-arch.ts` SHA-256, so a 3PAO can weigh coverage per cloud (AWS 10 / GCP 13 / Azure 11 expectations this build).
- **C-C9-6**: Baseline drift between emit-time and 3PAO sample-time (covered by C-C1-2). — MITIGATED 2026-07-11 (`TBDCOMMIT`): §6 Baseline Maintenance states the review cadence + on-change triggers + CM-2(2) automation (re-derived every run); the provenance footer records the inventory + reference-arch SHA-256s so the emit-time baseline is reconstructable. Drift detection over time is LOOP-E's job (this slice `blocks` LOOP-E ConMon baseline-drift).
- **C-C9-7**: CIS Benchmarks license — cannot copy verbatim. — MITIGATED 2026-07-11 (`TBDCOMMIT`): §4 + the provenance footer CITE the CIS Benchmarks URL + note the reference-arch content is original prose derived from CIS; no CIS content is pasted. Per REO Rule 3, cloud-vendor service names are allowed fixed data; baseline images/configs come from the real inventory + reference-arch source, not hardcoded in the emitter.
- **C-C9-8** (NEW 2026-07-11, `TBDCOMMIT`): **`providers/*/reference-arch.ts` are live SDK collectors, not static baseline arrays.** The per-slice Schemas §3 imagined `reference-arch.ts` exporting arrays of `{component, baselineImage, baselineConfig, controls}` records. The REAL files are async collectors that emit `finding({rule, passed, severity, current, target:{summary,rationale}, gap, remediation, nist_controls})` blocks against live cloud SDKs. §4 therefore greps each finding block for `(rule, target.summary, nist_controls, severity)` — the grep-against-source idiom (roe-emit.ts `readKsiScope` over ksi-map.ts) — which also keeps cloud SDK clients out of the docx emit path. Severity: low. Status: resolved (grep reader matches the real emitter shape; multi-cloud parse tested).
- **C-C9-9** (NEW 2026-07-11, `TBDCOMMIT`): **`scg-comparator.ts` compares a settings-map to observed settings, not inventory rows to reference rows.** The per-slice §5/§6 assumed scg-comparator would produce the inventory-vs-reference deviation summary. The REAL `compareScg(input)` takes `{guide:{settings:map}, observed:map}` and yields per-setting pass/fail (the SCG RECOMMENDED-config compare). Rather than force inventory rows into that model, C.C9 computes §5 with a local pure `diffInventoryVsReference` (two honest, source-grounded deviation kinds — undocumented-hardening-baseline + encryption-at-rest-disabled — severity from the anchoring finding), and cites scg-comparator in §2 as the complementary sibling. No re-ship (it already exists). Severity: low. Status: resolved (local pure diff; documented divergence).
- **C-C9-10** (NEW 2026-07-11, `TBDCOMMIT`): **DEPENDENCY-GRAPH lists C.C9 as depending on D.D1–D.D3 (diagrams); the per-slice-doc contract does not.** `docs/DEPENDENCY-GRAPH.md` records `C.C9 ← D.D1, D.D2, D.D3` (and `D.D1 --> C.C9`), which would have blocked C.C9 behind LOOP-D. The C.C9 per-slice-doc frontmatter `depends_on` (the implementation contract Phase −1 validates against) lists only docx-primitives + inventory chain + `reference-arch.ts` (+ soft-dep scg-comparator, present) — and the build confirms C.C9 §4 Reference Architecture is grep-read from `providers/*/reference-arch.ts` SOURCE, not from D.D1's visual boundary diagram. C.C9 shipped ahead of LOOP-D per its own contract; the STATUS "Next priority" note records the reconciliation. Follow-up: reconcile DEPENDENCY-GRAPH's C.C9 dependency row when LOOP-D is scheduled. Severity: low. Status: resolved (shipped per per-slice-doc contract; graph note pending LOOP-D).
- **C-C9-11** (NEW 2026-07-11, `TBDCOMMIT`): **Pre-slice `core/docx-primitives.ts` still not extracted** (extends C-C1-6 … C-C8-10 / C-X-1). C.C9 keeps local OOXML constants (`para`/`heading`/`table`/`fieldTable`/`stylesXml`/`CONTENT_TYPES`/`ROOT_RELS`/`DOC_RELS`) like the 14 shipped docx emitters — now **15** to migrate when C-X-1 lands (adds `baseline-config-emit`). Severity: medium. Status: pending (refactor deferred). Q6 (validate the operator `baselineReviewCadence` against the SSP CM-2 control-implementation entry) is likewise deferred to a future cross-check enhancement (no SSP CM-2 reader wired this slice).

---

## External dependencies that may change

| Dependency | Current Version | Used By | Risk Trigger | Watch URL |
|---|---|---|---|---|
| FedRAMP SSP Appendix G ISCP Template | Rev5 (May 2024) | C.C2 | New Rev release; section reordering | https://www.fedramp.gov/rev5/documents-templates/ |
| FedRAMP SSP A04 PIA Template | Rev4 (June 2019) | C.C4 | Rev5 release (FedRAMP help-desk article 28907995813275 says "no current plans" — but may change) | https://www.fedramp.gov/assets/resources/templates/ |
| FedRAMP SSP A10 FIPS 199 Template | Rev4 (June 2019) | C.C5 | Rev5 release | https://www.fedramp.gov/resources/documents/rev4/ |
| FedRAMP ConMon Playbook | v1.0 (2025-11) | C.C6 | New cadence; new reporting endpoint | https://www.fedramp.gov/resources/documents/Continuous_Monitoring_Playbook.pdf |
| FedRAMP ConMon Strategy Guide | v3.2 (2018-04) | C.C6 | Major rev (likely with Rev5 maturity) | https://www.fedramp.gov/assets/resources/documents/CSP_Continuous_Monitoring_Strategy_Guide.pdf |
| FedRAMP Agency Authorization Playbook | v4.1 (2025-11) | C.C8 | New rev with timeline updates | https://www.fedramp.gov/resources/documents/Agency_Authorization_Playbook.pdf |
| FedRAMP Initial Authorization Package Checklist | (current) | C.C8 | New artifacts added | https://www.fedramp.gov/assets/resources/templates/FedRAMP-Initial-Authorization-Package-Checklist.xlsx |
| FedRAMP Incident Communications Procedures | (CSP_Incident_Communications_Procedures.pdf) | C.C3 | SLA changes; CISA endpoint change | https://www.fedramp.gov/assets/resources/documents/CSP_Incident_Communications_Procedures.pdf |
| RFC-0026 (CA-7 Clarification) | initial (2025) | C.C6 | Final-state language | https://www.fedramp.gov/rfcs/0026/ |
| NIST SP 800-34 | Rev. 1 Upd Nov 2010 | C.C2 | Rev. 2 in some future | https://csrc.nist.gov/pubs/sp/800/34/r1/upd1/final |
| NIST SP 800-37 | Rev. 2 (Dec 2018) | C.C7, C.C8 | Rev. 3 | https://csrc.nist.gov/pubs/sp/800/37/r2/final |
| NIST SP 800-39 | (March 2011) | C.C7 | Future rev | https://nvlpubs.nist.gov/nistpubs/legacy/sp/nistspecialpublication800-39.pdf |
| NIST SP 800-53 | Rev. 5 Upd Dec 2020 | All | Rev. 5.2.0+ patch | https://csrc.nist.gov/pubs/sp/800/53/r5/upd1/final |
| NIST SP 800-53A | Rev. 5 (Jan 2022) | C.C6 | Rev. 5.2.0+ | https://csrc.nist.gov/pubs/sp/800/53/a/r5/final |
| NIST SP 800-60 | Vol 2 Rev. 1 (Aug 2008) | C.C5 | Rev. 2 (IWD Jan 2024) — when final, catalog re-extract | https://csrc.nist.gov/pubs/sp/800/60/v2/r1/final |
| NIST SP 800-61 | Rev. 3 (Apr 2025) | C.C3 | Future rev; Rev. 2 retro-references | https://csrc.nist.gov/pubs/sp/800/61/r3/final |
| NIST SP 800-128 | Upd Oct 2019 | C.C1, C.C9 | Rev. 2 | https://csrc.nist.gov/pubs/sp/800/128/upd1/final |
| NIST SP 800-137 | (Sept 2011) | C.C6 | Rev. 1 | https://csrc.nist.gov/pubs/sp/800/137/final |
| NIST SP 800-137A | (May 2020) | C.C6 | Rev. updates | https://nvlpubs.nist.gov/nistpubs/SpecialPublications/NIST.SP.800-137A.pdf |
| NIST Privacy Framework | v1.0 (Jan 2020) | C.C4 | v2.0 | https://www.nist.gov/privacy-framework |
| FIPS 199 | (Feb 2004) | C.C5 | Successor (no public timeline) | https://nvlpubs.nist.gov/nistpubs/FIPS/NIST.FIPS.199.pdf |
| FIPS 200 | (March 2006) | C.C5 | Successor | https://nvlpubs.nist.gov/nistpubs/FIPS/NIST.FIPS.200.pdf |
| CISA BOD 22-01 (KEV catalog) | active | C.C6 escalation defaults | New dueDate semantics | https://www.cisa.gov/news-events/directives/binding-operational-directive-22-01 |
| OMB M-03-22 (E-Government §208) | active | C.C4 | Successor memo | https://www.whitehouse.gov/wp-content/uploads/legacy_drupal_files/omb/memoranda/2003/m03_22.pdf |
| CIS Benchmarks | rolling | C.C9 indirect | Benchmark updates per service | https://www.cisecurity.org/cis-benchmarks |
| OSCAL 1.1.2 schemas | 1.1.2 | C.C5 indirect (SSP cross-ref) | 1.2.x release | https://pages.nist.gov/OSCAL-Reference/models/v1.1.2/ |

### Upstream library updates
- **ajv**: schema validator (used by `core/oscal-validate.ts`); 8.x → 9.x potential breaking change.
- **pino**: structured logging; minor releases.
- No `docx` npm package used — all .docx are hand-built via `core/zip.ts` `zipStore`. Reduces dependency risk.

---

## Resolved risks (historical)

Empty initially — populated as risks are resolved. Pattern for an entry:

```
| ID | Description | Severity | Resolution | Resolved Date | Resolved Commit |
|---|---|---|---|---|---|
| C-X-N | <description> | <sev> | <resolution note> | YYYY-MM-DD | <hash> |
```

---

## Notes for implementing sessions

1. **Add new risks here** as you encounter them during implementation — preserve evidence (commit, run log) in the description.
2. **Cross-cutting risks (C-X-N)** should be addressed in the pre-slice (`core/docx-primitives.ts`) before C.C1 ships. Pre-slice is the right place to fix shared problems once.
3. **When a risk is resolved**, MOVE it to "Resolved risks (historical)" with the resolution note + commit hash. Don't delete history.
4. **Severity escalation**: if a `medium` risk causes a 3PAO finding or a delivery delay, escalate to `high` and document the trigger.
5. **External-dependency check**: monthly cron (proposed) checks every URL in the "External dependencies" table for HTTP 200/304; opens an issue when a 200 appears that wasn't there before (indicates revision).
